<section>
  <h1>Shows</h1>
	{{#if hasError}}
	<h3>SEARCH TERM IS EMPTY!</h3>
	{{/if}}
  {{#each shows}}  
    <article class="shows">
      <h2><a href="/show/{{show.id}}">{{show.name}}</a></h2>
    </article>
  {{/each}}
	<br>
	  <a href="/">Back to All Shows</a>
</section>
