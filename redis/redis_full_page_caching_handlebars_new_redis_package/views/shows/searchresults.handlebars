<section>
  <h1>Shows</h1>
	{{#if hasError}}
	<h3>SEARCH TERM IS EMPTY!</h3>
	{{/if}}
  {{#each shows}}  
    <article class="shows">
      <h2><a href="/shows/{{show.id}}">{{show.name}}</a></h2>
    </article>
  {{/each}}
	<br>
	  <a href="/shows">Back to All Shows</a>
</section>
