{
	"name": "redis-demo",
	"version": "1.0.0",
	"description": "A worker role for our recipe creation",
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Phil Barresi",
	"license": "MIT",
	"dependencies": {
		"express": "^4.14.1",
		"flat": "^2.0.1",
		"redis": "^4.3.1"
	}
}
